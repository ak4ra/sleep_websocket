## live: https://thawing-wildwood-44579.herokuapp.com/

A Java/Spring app that sends scheduled HTTP requests to a few heroku addresses, taking them out of sleep mode, and uses a websocket connection to send the status code along with the URL to a React frontend.
